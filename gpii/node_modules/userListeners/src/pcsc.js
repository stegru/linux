/*
 * PC/SC User listener.
 *
 * Copyright 2017 Raising the Floor - International
 *
 * Licensed under the New BSD license. You may not use this file except in
 * compliance with this License.
 *
 * The R&D leading to these results received funding from the
 * Department of Education - Grant H421A150005 (GPII-APCP). However,
 * these results do not necessarily represent the policy of the
 * Department of Education, and you should not assume endorsement by the
 * Federal Government.
 *
 * You may obtain a copy of the License at
 * https://github.com/GPII/universal/blob/master/LICENSE.txt
 */

"use strict";

var fluid = require("infusion");

var gpii = fluid.registerNamespace("gpii");
var Pcsclite = require("@pokusew/pcsclite");

fluid.registerNamespace("gpii.userListeners");

fluid.defaults("gpii.userListeners.pcsc.linux", {
    gradeNames: ["fluid.component"],
    members: {
        proximity: true,
        listenerName: "PC/SC"
    },
    invokers: {
        getPCSCLite: {
            funcName: "gpii.userListeners.getPCSCLite",
            args: "{that}"
        }
    }
});

/**
 * Gets a PCSCLite instance.
 *
 * @param that {Component} A gpii.pcscUserListener instance.
 * @return {PCSCLite} A new instance of PCSCLite, or null if there's no reader.
 */
gpii.userListeners.getPCSCLite = function () {
    var pcscLiteInstance = null;
    pcscLiteInstance = new Pcsclite();
    return pcscLiteInstance;
};
